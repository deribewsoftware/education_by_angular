import { NextDirectDirective } from './next-direct.directive';

describe('NextDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new NextDirectDirective();
    expect(directive).toBeTruthy();
  });
});
