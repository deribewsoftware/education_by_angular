import { PrevDirectDirective } from './prev-direct.directive';

describe('PrevDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevDirectDirective();
    expect(directive).toBeTruthy();
  });
});
